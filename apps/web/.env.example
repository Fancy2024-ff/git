# Dashboard 前端环境配置（复制为 .env.development 或 .env.production）

# 后端 API 地址
VITE_API_BASE_URL=http://localhost:8000
# 后端 WebSocket 地址
VITE_WS_BASE_URL=ws://localhost:8000
# 与后端 DASHBOARD_API_KEY 一致的 token。
# 生产环境必填；通过 X-API-Key header 和 WS ?token= 传递。
VITE_API_TOKEN=
