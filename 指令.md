下一阶段任务：完善小程序生成板块，把 5 套传播型模板从“可构建骨架”升级为“可交互本地闭环 Demo”。

不要动大架构，不要恢复 agents，不要引入 generator service。
继续遵守 capability-domain 架构。

核心目标：
每个生成出来的小程序，不只是能 build，还要能在前端完成一条本地闭环：
输入内容/上传占位 → 生成结果 → 结果页展示 → 分享 CTA → 分享解锁/去水印占位 → 回流提示。

重点不是接真实 AI API，而是先把生成产物变成“可演示、可体验、可继续接真实后端”的业务骨架。

具体要求：

1. 统一生成产物里的 runtime service
在生成出来的小程序中增加统一的本地服务层，例如：
src/services/generation.ts 或 src/services/ai.ts

要求：
- 不写真实 key
- 支持 mockGenerate()
- 支持按模板类型返回不同 mock result
- 支持后续切换到真实 API endpoint
- API endpoint 通过配置占位，不硬编码真实服务

2. 5 类模板分别有可交互闭环
avatar-viral：
- 输入/上传照片占位
- 生成头像结果占位
- 结果页展示头像风格
- 分享解锁高清/去水印

sticker-viral：
- 输入文字/表情主题
- 生成一组 sticker mock
- 展示 sticker pack
- 分享解锁更多表情

pet-talk-viral：
- 上传宠物图占位
- 输入一句台词
- 生成宠物说话视频 mock
- 分享给好友/朋友圈占位

funny-video-viral：
- 输入搞笑主题/人物关系
- 生成 15 秒短视频脚本/分镜 mock
- 结果页展示分镜和分享文案
- 分享挑战/接龙占位

blessing-video-viral：
- 输入收礼人姓名/节日/祝福语
- 生成祝福视频/贺卡 mock
- 结果页展示祝福卡片
- 群发/转发祝福占位

3. base/result 页面升级
core/generator/src/templates/base/src/pages/result/result.vue 需要成为所有模板可复用的结果展示页：
- 读取生成结果
- 展示 result title/preview/share copy
- 有分享按钮
- 有“分享解锁高清/去水印/更多模板”的状态
- 状态可以本地 mock，不需要后端

4. 表单页升级
base/form 页面或模板 overlay 表单页要能：
- 收集输入
- 调用 mockGenerate
- 跳转 result
- 不再只是静态页面

5. 生成数据契约
定义一个清晰的 GeneratedResult 类型，例如：
- id
- template
- title
- previewType
- previewData
- shareTitle
- shareCopy
- unlockHint
- watermarkEnabled
- createdAt

放在生成项目内，或模板内统一 source。
要求不要破坏 build。

6. GrowthQA 升级
GrowthQA 不只查有没有分享 CTA，还要查：
- 有 generation service
- 有 result page
- 有 share CTA
- 有 unlock hook
- 有 watermark/unlock 文案
- 5 套模板至少各有一条传播链路

7. EngineeringQA 不允许降级
必须继续真实：
npm install
npm run build:mp-weixin
dist/build/mp-weixin 校验
不能跳过 build，不能假通过。

8. 测试
新增或更新测试：
- Python generator consistency：生成项目包含 generation service、result page、无未填 token
- GrowthQA tests：缺 share/unlock/service 时失败
- Viral template real build：5 套模板仍全部 build 通过
- Node parity test：如果 page-builder.ts 需要同步，也同步 vitest

9. 前端控制台摘要
apps/web 控制台或交付物面板中补充生成产物摘要：
- selected_template
- generated result flow 是否存在
- growth hooks 是否存在
- QA 中 generation_flow_passed 是否通过

10. 验收命令
必须跑：
python -m pytest core apps
cd core/generator && npm.cmd test -- --run
cd apps/web && npm.cmd test -- --run
cd apps/web && npm.cmd run build
$env:RUN_BUILD_TESTS="1"; python -m pytest core/generator/tests/test_viral_build.py
python core/pipeline/runner.py --mode demo --job-id interactive-demo-check
python core/pipeline/runner.py --mode real --job-id interactive-real-check

验收报告必须给：
- git status --short
- 改了哪些文件
- 5 套模板各自的交互闭环说明
- demo/real pipeline 的 selected_template
- qa-report/growth-qa-report 关键字段
- 是否有任何 build 失败
- 是否有任何未提交的临时产物