# 进程

> 项目：小程序传播型机会工厂（repo-review）
> 分支：`refactor/core-capability-domains`
> 最后更新：2026-06-18

本文件记录本次「架构收口 + 传播型能力完善」交付的全过程、最终状态与真实边界，便于后续接手继续开发。

---

## 一、最终架构

采用 **capability-domain（能力域）架构**，已彻底取代旧的 agents 架构。

```
apps/
  api/        统一后端入口（FastAPI）
  web/        前端控制台（Vue 3 + Vite）
core/
  pipeline/       只做 step 编排（日志/报告/异常封装）
  opportunity/    机会发现 + Viral Score + 题材归类/选模板
  generator/      PRD + 唯一代码生成真源 codegen.py + 模板工厂
  growth/         增长计划 + 分享/裂变策略
  qa/             工程 QA / 增长 QA / 合规 QA / 提交就绪
  publisher/      上架材料 + 提交包 + Telegram 部署
  platforms/      平台规则与差异
  integrations/   LLM 等外部服务接入
  runtime/        config / context / artifacts / database / manifest
  shared/         跨域 schema / types
data/
  inputs/real/apps.json   真实输入唯一 canonical 路径
  outputs/{jobId}/        每次运行产物（git 忽略）
infra/docker/             Dockerfile.api / Dockerfile.web
```

### 关键收口点
- 后端唯一入口 `apps/api/main.py`；流水线唯一入口 `core/pipeline/runner.py`。
- 代码生成唯一执行真源 `core/generator/codegen.py`；Node `src/index.ts` / `page-builder.ts` 仅作 vitest/parity 兼容壳，不在生产链路。
- 运行时协议只用 `step` / `capability`，**完全无 `agent` 字段**。
- 真实输入唯一路径 `data/inputs/real/apps.json`，无 `data/real_inputs` fallback。
- 部署仅 `api + dashboard` 两个服务，移除 generator service 与 `GENERATOR_URL` / `GENERATOR_API_KEY`。
- 旧目录 `agents/` `core/agents/` `generator/` `dashboard/` `scripts/` 全部移除。

---

## 二、流水线步骤（13 必跑 + 1 可选）

| 步 | capability | 真源 |
|----|-----------|------|
| 1  | MarketInput | runner.load_market_input + opportunity/scrapers |
| 2  | DemandAnalysis | opportunity/demand_analysis.py（选候选 demand×0.6 + viral×0.4） |
| 3  | GapCheck | opportunity/gap_analysis.py |
| 4  | ViralScore | opportunity/viral_score.py（8 维）+ classifier.py（选模板） |
| 5  | OpportunityScore | opportunity/scoring.py（viral 权重 0.25，最高） |
| 6  | PRD | generator/prd_builder.py |
| 7  | Codegen | generator/codegen.py（唯一真源）+ src/templates |
| 8  | PublishMaterials | publisher/materials.py |
| 9  | Growth | growth/planner.py + share_strategy.py |
| 10 | PublishPackage | publisher/package_builder.py + platforms/guides.py |
| 11 | EngineeringQA | qa/engineering_qa.py（真 npm install + build + dist） |
| 12 | GrowthComplianceQA | qa/growth_qa.py + compliance_qa.py |
| 13 | Readiness | qa/readiness.py |
| 14（可选） | TelegramDeploy | publisher/telegram_deploy.py（需 telegram 平台 + 双 token） |

`pipeline-report.json` 实测 demo / real 均 `steps.length = 13`。

---

## 三、传播型能力升级

### 选题方向（优先品类）
AI 头像/写真、表情包/Sticker、宠物说话视频、搞笑视频、祝福视频。

### Viral Score（8 维度）
share_trigger / social_proof / low_friction / reward_loop / emotion /
content_reusability / watermark_tolerance / unlock_mechanism_fit（权重和 = 1.0）。
真正参与决策：候选选择占 40%，机会总分占 25%（最高权重维度）。

### 模板工厂（5 套传播型模板，全部真实可构建）
| 模板 | 签名页 |
|------|--------|
| avatar-viral | gallery |
| sticker-viral | pack |
| pet-talk-viral | upload |
| funny-video-viral | clip |
| blessing-video-viral | greeting |

codegen 采用 base 骨架 + 题材 overlay，自动把签名页注册进 pages.json。
base 结果页内置「分享解锁 / 去水印」裂变钩子，所有生成产物自带传播原语。

### 增长产物（每次生成必产）
`growth-plan.md`（目标用户/冷启动/裂变/钩子/激励/渠道/指标）、`share-strategy.md`（分享去向/带品牌/分享后解锁/降阻力/防广告感）。

### QA 三层
- **EngineeringQA**（阻塞）：真跑 npm install + build:mp-weixin，断言 dist app.js/json/wxss + 页面 js；严格 UTF-8 + U+FFFD + 模式乱码检测。
- **GrowthQA**（阻塞）：增长产物存在 + markdown 要素 + 生成代码传播链路（分享 CTA / 解锁钩子 / 结果页）。
- **ComplianceQA**：隐私政策/用户协议/审核备注（硬闸）+ 过度营销词/敏感词（warning 级，不阻塞）。

### 前端可视化
DecisionOverview（机会分 / Viral 8 维 / 选中模板 / 就绪度）、DeliverablesPanel 增长交付物区（growth-plan / share-strategy / growth-qa）、FactoryConsole 动态步数 + viral/QA 摘要。

---

## 四、验证结果（提交前 + 提交后均通过）

| 命令 | 结果 |
|------|------|
| `python -m pytest core apps` | 71 passed, 5 skipped |
| `cd core/generator && npm test -- --run` | 13 passed |
| `cd apps/web && npm test -- --run` | 4 passed |
| `cd apps/web && npm run build` | built ✓ |
| `RUN_BUILD_TESTS=1 pytest core/generator/tests/test_viral_build.py` | 5 passed（5 模板真实构建） |
| `runner.py --mode demo` | exit 0，三层 QA 通过 |
| `runner.py --mode real` | exit 0，命中 funny-video-viral，三层 QA 通过 |

产物摘要：
- demo → AI Photo Enhancer / ai-image / viral 76.9 high / opportunity 82.4
- real → Funny Clip Maker / funny-video-viral / viral 90.8 high / opportunity 83.1

---

## 五、提交记录（本地，未 push）

分支 `refactor/core-capability-domains`，4 个 commit，共 199 文件，工作树 clean。

```
ae34af2 web-docs: expose artifacts and align documentation
876008b growth-qa: add viral decision, growth docs, three-layer QA
fd34e35 generator: add viral template factory and canonical codegen
7a06724 arch: collapse to capability-domain runtime
```

未提交：data/outputs、data/platform-auth/*.json。已忽略：node_modules、dist、.npm-cache、__pycache__。

---

## 六、真实边界（如实声明，未做的不包装成已做）

1. **规则版 v1，非 LLM 语义机会发现**：viral_score / opportunity / classifier 为关键词加权 + 规则打分（`data_source: demo_rule_based`），对描述用词敏感，无语义理解。
2. **模板是可构建骨架，非真实 AI 生成后端**：5 套模板真实通过构建，但页面 AI 能力 / 分享解锁是前端交互与占位数据，需接真实后端 API 才产出真实素材。
3. **是提交准备包，非一键平台审核通过**：产出提交包 + 合规材料 + 就绪评估；真实上传（miniprogram-ci 等）与平台审核仍需人工/密钥；敏感词为有限词表的 warning 级，不替代平台审核。

---

## 七、后续可继续的方向

- 把规则版评分替换/增强为 LLM 语义机会发现与题材归类。
- 为模板接入真实生成后端（图像/视频/配音 API）。
- 打通 miniprogram-ci 等平台自动上传与审核状态回调。
- 扩充敏感词/合规词库，或接入平台官方校验。
